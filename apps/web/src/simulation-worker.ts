/**
 * Local worker entry — re-exports the simulation worker.
 *
 * This file exists inside the Vite project root so that
 * `?worker` import resolution works correctly.
 */

export * from '@faultlab/simulation-client/worker'
